import { ToastContainer } from 'react-toastify'
import { Routing } from '@/common/routing'

export const App = () => {
    return (
        <>
            <Routing />

            <ToastContainer />
        </>
    )
}
